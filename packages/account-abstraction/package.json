{
  "name": "account-abstraction",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "dev": "wait-port localhost:8545 && pnpm run deploy-contracts && pnpm run start:alto",
    "start:alto": "pushd node_modules/alto; pnpm install; popd; pnpm alto run --config ./config.localhost.json",
    "deploy-contracts": "tsx scripts/deployContracts.ts"
  },
  "dependencies": {
    "alto": "git+https://github.com/latticexyz/alto.git",
    "fastify": "^4.26.2",
    "typescript": "5.4.2",
    "viem": "2.7.12",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "tsx": "^4.7.1",
    "wait-port": "^1.0.4"
  }
}
