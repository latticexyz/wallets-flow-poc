{
  "name": "account-abstraction",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "dev": "wait-port 127.0.0.1:8545 && pnpm run deploy:contracts && pnpm run start:alto",
    "start:alto": "pushd node_modules/alto; pnpm install; popd; pnpm alto run --config ./config.localhost.json",
    "deploy:contracts": "tsx scripts/deployContracts.ts && pnpm run deploy:gas-tank",
    "deploy:gas-tank": "cd ./node_modules/@latticexyz/gas-tank/packages/contracts && pnpm install && pnpm mud deploy --rpc http://127.0.0.1:8545 --salt '0x00'"
  },
  "dependencies": {
    "@latticexyz/cli": "^2.0.4",
    "@latticexyz/gas-tank": "git+https://github.com/latticexyz/gas-tank.git",
    "alto": "git+https://github.com/latticexyz/alto.git",
    "fastify": "^4.26.2",
    "typescript": "5.4.2",
    "viem": "2.7.12",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "tsx": "^4.7.1",
    "wait-port": "^1.0.4"
  }
}
