procs:
  contracts:
    cwd: packages/contracts
    shell: pnpm run dev
  client:
    cwd: packages/client
    shell: pnpm run dev
  anvil:
    cwd: packages/contracts
    shell: anvil --base-fee 0
  account-abstraction:
    cwd: packages/account-abstraction
    shell: pnpm run dev
